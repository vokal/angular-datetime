"use strict";

describe( "Time Picker", function ()
{
    it( "should load", function ()
    {
        browser.get( "/" );
    } );

    it( "should run this test", function ()
    {
        expect( 1 + 2 ).toBe( 3 );
    } );

} );
